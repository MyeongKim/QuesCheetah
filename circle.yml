database:
  pre:
    - python manage.py makemigrations
    - python manage.py migrate
machine:
  python:
    version: 3.4.3
dependencies:
  pre:
    - "mysql -u ubuntu -e 'create database circle_test;'"